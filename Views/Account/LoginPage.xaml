<Page
    x:Class="SGSClient.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0, 20, 0, 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Logo -->
        <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="1" Source="/Assets/SGSLOGO.png"
               HorizontalAlignment="Center" VerticalAlignment="Center" Height="60" Width="60"
               Margin="0,0,10,0"/>

        <!-- Nagłówek "Login" -->
        <TextBlock Grid.Row="0" Grid.Column="1" Height="50" Margin="0,10,0,0"
                   Name="LoginHeading" VerticalAlignment="Center" HorizontalAlignment="Left"
                   FontSize="24" FontWeight="Bold" Text="Logowanie"/>

        <!-- Przycisk "Zaloguj przez Entra ID" -->
        <Button Grid.Row="1" Grid.Column="1" Content="Zaloguj przez Entra ID"
                Height="40" Margin="0,20,0,0" Name="buttonLogin" VerticalAlignment="Top"
                Click="ButtonLogin_Click" FontSize="16" Style="{StaticResource AccentButtonStyle}"
                BorderThickness="0" Width="220"/>

        <!-- Link do rejestracji -->
        <TextBlock Grid.Row="2" Grid.Column="1" Height="25" Margin="0,20,0,0"
                   VerticalAlignment="Top" FontSize="16" FontStyle="Italic" Padding="5">
            <Run Text="Nie masz konta? "/>
            <Hyperlink Click="ButtonRegister_Click" FontSize="16" FontStyle="Normal"
                       Foreground="#0078D4">
                <Run Text="Zarejestruj się"/>
            </Hyperlink>
        </TextBlock>

        <!-- Link "Zapomniałem hasła" -->
        <TextBlock Grid.Row="3" Grid.Column="1" Height="50" Margin="0,20,0,0"
                   VerticalAlignment="Top" FontSize="16" FontStyle="Italic" Padding="5">
            <Hyperlink Click="HyperlinkForgotPassword_Click" FontSize="16"
                       FontStyle="Normal" Foreground="#0078D4">
                <Run Text="Zapomniałem hasła"/>
            </Hyperlink>
        </TextBlock>
    </Grid>
</Page>
