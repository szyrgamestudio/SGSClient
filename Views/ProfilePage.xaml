<Page x:Class="SGSClient.Views.ProfilePage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="using:CommunityToolkit.WinUI.Controls" xmlns:models="using:SGSClient.Core.Models">

    <ScrollViewer>
        <ItemsControl x:Name="RecentlyPlayedItemsControl" Margin="0,12,0,12"
                      ItemsSource="{x:Bind ViewModel.RecentlyPlayed}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <controls:WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="models:UserGameInfo">
                    <Button Width="160" Height="240" Margin="0,0,14,14" Padding="12" HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource ControlElevationBorderBrush}"
                            BorderThickness="1" Click="ButtonGame_Click" CornerRadius="8"
                            Tag="{Binding GameSymbol}">
                        <StackPanel HorizontalAlignment="Center" Spacing="8">
                            <Image Width="80" Height="80" HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Source="{x:Bind LogoPath, Mode=OneWay}"
                                   Stretch="Uniform" />
                            <!--  Tytuł gry  -->
                            <TextBlock FontSize="14" FontWeight="SemiBold"
                                       Text="{x:Bind GameTitle}"
                                       TextAlignment="Center" TextWrapping="WrapWholeWords" />
                            <!--  Data ostatniego grania  -->
                            <TextBlock FontSize="12"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       Text="{x:Bind LastPlayedShort}"
                                       TextAlignment="Center" />
                            <!--  Czas gry  -->
                            <TextBlock FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       Text="{x:Bind PlayTimeShort}"
                                       TextAlignment="Center" />
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>

</Page>
